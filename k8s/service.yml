apiVersion: v1
kind: Service
metadata:
  name: google-shopping-service
spec:
  selector:
    app: google-shopping
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
