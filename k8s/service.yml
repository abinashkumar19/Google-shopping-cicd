apiVersion: v1
kind: Service
metadata:
  name: google-shopping-service
  namespace: default
spec:
  selector:
    app: google-shopping
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
