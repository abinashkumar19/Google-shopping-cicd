apiVersion: v1
kind: Service
metadata:
  name: offers-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  selector:
    app: offers
  ports:
    - port: 80
      targetPort: 80
