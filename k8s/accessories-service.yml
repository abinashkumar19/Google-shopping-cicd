apiVersion: v1
kind: Service
metadata:
  name: accessories-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  selector:
    app: accessories
  ports:
    - port: 80
      targetPort: 80
